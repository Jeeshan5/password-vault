# MongoDB Connection - REQUIRED
# For local development: mongodb://localhost:27017/password-vault
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/password-vault
MONGODB_URI=mongodb://localhost:27017/password-vault

# Environment Configuration for Password Vault
# Copy this file to .env.local and fill in your actual values

# MongoDB Connection String
# Get this from your MongoDB Atlas cluster connection settings
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/password-vault?retryWrites=true&w=majority

# JWT Secret Key (generate a secure random string)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-jwt-secret-here-change-this

# Database Name
DATABASE_NAME=password-vault

# Next.js Environment
NODE_ENV=development

# Optional Configuration
DB_NAME=password-vault
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-key

# Production API URL (uncomment for production)
# NEXT_PUBLIC_API_URL=https://yourapp.com/api

# Email Settings (optional - for future features)
# EMAIL_SERVER_HOST=smtp.gmail.com
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER=your-email@gmail.com
# EMAIL_SERVER_PASSWORD=your-app-password
# EMAIL_FROM=your-email@gmail.com